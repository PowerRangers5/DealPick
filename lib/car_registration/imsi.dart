import 'package:flutter/material.dart';

class Imsi extends StatelessWidget {
  const Imsi({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('sadas'),
      ),
      body: Center(
        child: Text('dkssud'),
      ),
    );
  }
}
