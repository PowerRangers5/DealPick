import 'package:flutter/material.dart';

class CategoryPage extends StatelessWidget {
  final int selectedCategory;

  const CategoryPage({
    super.key,
    required this.selectedCategory,
  });

  @override
  Widget build(BuildContext context) {
    return Container(
      // 카테고리 페이지 내용을 여기에 구현하세요
    );
  }
} 